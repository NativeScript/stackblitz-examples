import { nativescriptRootLayout } from './nativescript-root-layout';

describe('nativescriptRootLayout', () => {
    it('should work', () => {
        expect(nativescriptRootLayout()).toEqual('nativescript-root-layout');
    })
})